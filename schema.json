{
  "$ref": "#/definitions/LLMGatewayConfig",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "definitions": {
    "LLMGatewayConfig": {
      "additionalProperties": false,
      "description": "User-facing config schema (sparse — only set keys are persisted).",
      "properties": {
        "baseUrl": {
          "description": "Override the base URL for self-hosted LLM Gateway instances. Defaults to \"https://api.llmgateway.io/v1\".",
          "type": "string"
        },
        "includeDeactivated": {
          "description": "When true, include models marked as deactivated in the model list. Deactivated models may be removed from the gateway at any time. Defaults to false.",
          "type": "boolean"
        },
        "routing": {
          "description": "Provider routing strategy. Controls which upstream provider the gateway selects for auto-routed model IDs.\n\n- \"auto\"       Full weighted smart-routing score (default).\n- \"price\"      Optimise for lowest cost.\n- \"throughput\" Optimise for highest throughput.\n- \"latency\"    Optimise for lowest latency (streaming requests only).\n\nNote: on coding (dev) plans only \"auto\" and \"price\" are available.",
          "enum": [
            "auto",
            "price",
            "throughput",
            "latency"
          ],
          "type": "string"
        },
        "webSearch": {
          "description": "Enable native web search for models that support it. When true, the gateway instructs the model to search the web for real-time information.",
          "type": "boolean"
        }
      },
      "type": "object"
    }
  }
}